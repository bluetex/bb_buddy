name: test me
on:
  workflow_dispatch:
    branches: [ main ]    
    
jobs: 
  this is a job: 
    steps:
      - name: a shell step
        shell: sh
        run: |
          label="MERGE\nrelease/dubai"
          echo $label
      
